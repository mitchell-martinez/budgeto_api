# ─────────────────────────────────────────────────────────────────────
# VPS Environment Variables
# Copy to ~/budgeto/.env on your Mammoth Cloud VPS and fill in real values.
# This file is git-ignored — secrets live only on the server.
# ─────────────────────────────────────────────────────────────────────

# PostgreSQL (used by both the db container and the API)
POSTGRES_USER=budgeto
POSTGRES_PASSWORD=CHANGE_ME_generate_a_strong_password
POSTGRES_DB=budgeto

# API
DATABASE_URL=postgresql://budgeto:CHANGE_ME_generate_a_strong_password@db:5432/budgeto
JWT_SECRET=CHANGE_ME_generate_a_random_string_at_least_32_characters
CORS_ORIGIN=https://budgeto.app
NODE_ENV=production
PORT=4000
