Use npm run commands, not npx, for running scripts defined in package.json
Do not update package-lock directly, always update or add packages via package.json and then run npm install to update the lock file
